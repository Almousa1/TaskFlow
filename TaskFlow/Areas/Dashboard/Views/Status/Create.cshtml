@model StatusCreateVM
@{
    ViewData["Title"] = DashboardResource.AddStatus;
}
<div class="container-fluid py-4">
    <div class="card border-0 shadow-sm">
        <div class="card-header bg-white border-bottom">
            <h5 class="mb-0">@DashboardResource.AddStatus</h5>
        </div>
        <div class="card-body">
            <form asp-area="Dashboard" asp-controller="Status" asp-action="Create" method="post" novalidate>
                @Html.AntiForgeryToken()
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="StatusName" class="form-label"></label>
                        <input asp-for="StatusName" class="form-control" />
                        <span asp-validation-for="StatusName" class="text-danger"></span>
                    </div>
                    <div class="col-md-6 mb-3">
                        <label asp-for="StatusNameAr" class="form-label"></label>
                        <input asp-for="StatusNameAr" class="form-control" />
                        <span asp-validation-for="StatusNameAr" class="text-danger"></span>
                    </div>
                </div>
                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check2"></i> @DashboardResource.Save
                    </button>
                    <a asp-area="Dashboard" asp-controller="Status" asp-action="Index" class="btn btn-outline-secondary ms-2">@DashboardResource.Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}